<div class='groupings'>
  <h1><%= resource.title %></h1>
  <%= render resource.grouping_parameters %>
  <%= link_to "edit", edit_resource_path %>

  <%= link_to "segments", mine_grouping_grouping_segments_path(@mine, @grouping) %>
</div>
