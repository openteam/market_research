<h1>
  <% @grouping_segment.ancestors.each do |ancestor| %>
    <%= ancestor.title %>
  <% end %>
  <%= @grouping_segment.title %>
</h1>
<h2>Список учреждений</h2>
<table>
  <tr>
    <th>Название</th>
    <th>Адрес</th>
    <th>Телефон</th>
    <th>Email</th>
  </tr>
  <% @grouping_segment.items.each do |item| %>
    <tr>
      <%= render partial: 'items/item', locals: {item: item} %>
    </tr>
  <% end %>
</table>
<%= link_to "back", mine_grouping_grouping_segments_path(@mine, @grouping) %>
